@{
    ViewData["Title"] = "Demo Configuration";
    var missingConfigs = ViewBag.MissingConfigs as List<string>;
}

<div class="container">
    <h1 class="display-4">Application Configuration Steps</h1>

    @if (missingConfigs != null && missingConfigs.Count > 0)
    {
        <div class="alert alert-warning">
            <h4>Warning:</h4>
            <ul>
                @foreach (var config in missingConfigs)
                {
                    <li>@config</li>
                }
            </ul>
            <p>Please ensure all required configuration settings are provided.</p>
        </div>
    }

    <p>
        Follow the steps below to configure the application:
    </p>
    <ol>
        <li>Set up your Azure Storage account and create a blob container.</li>
        <li>Store the required configuration values (like connection strings) in environment variables or in the appsettings.json file.</li>
        <li>Azure Blob Storage connection string ["AzureStorage:ConnectionString"] </li>
        <li>Azure Blob container name ["AzureStorage:ContainerName"] configuration</li>
        <li>Azure Blob name ["AzureStorage:BlobName"] configuration </li>
        <li>Redis Cache connection string ["RedisCache:ConnectionString"] </li>
        
        
        <li>Ensure that the Redis Cache service is properly configured and accessible.</li>
        <li>Deploy the application to Azure App Service or your preferred hosting environment.</li>
        <li>Use the "Refresh Blob Content" view to load the initial content into the cache.</li>
        <li>Use the "Edit Blob Content" view to update the content of the blob as needed.</li>
    </ol>
</div>
